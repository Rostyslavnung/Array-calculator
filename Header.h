#pragma once

#include "Size.h"

void fillA(int array[size]);
void print_Array(int array[size]);
void fillC(int A[size], int C[size]);
void conditionalFunc(int A[size], int C[size], int isOdd = 0);